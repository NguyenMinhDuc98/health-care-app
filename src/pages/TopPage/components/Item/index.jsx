import React from "react";

const Item = ({ image, date, status }) => {
  console.log(
    "🚀 ~ file: index.jsx:4 ~ Item ~ image, date, status:",
    image,
    date,
    status
  );
  return <div>ListItem</div>;
};

export default Item;
